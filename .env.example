# Porta del server (default: 3001)
PORT=3001

# Percorso del database SQLite
# In produzione punta a un volume persistente
# es. Railway: /data/bfp.db  |  Render: /var/data/bfp.db
DATABASE_PATH=./server/bfp.db

# Ambiente (development | production)
NODE_ENV=development
